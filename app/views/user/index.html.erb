<!DOCTYPE html>
<html>
  <head>
    <title>Welcome! Please sign up!</title>
  </head>

  <body>
    <div class="SignupBox">
    <% if @user_signedup %>
      <h2>You are signed in!</h2>
    <% else %>
      <h2>Sign up to create your site now!</h2>
      <% if flash[:notice] %>
        <div class="ErrorMessage"><%= flash[:notice] %></div>
      <% end %>
      <%= form_tag("/user", method: "post") do %>
        <p>
          <%= label_tag(:name, "Name:") %>
          <%= text_field_tag :name %>
        </p>

        <p>
          <%= label_tag(:email, "Email:") %>
          <%= text_field_tag :email %>
        </p>

        <p>
          <%= label_tag(:password, "Password:") %>
          <%= password_field_tag :password %>
        </p>

        <p>
          <%= submit_tag "Sign up!" %>
        </p>
      <% end %>
    <% end %>
    </div>
  </body>
</html>
